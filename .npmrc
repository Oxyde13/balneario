# Projeto pessoal: usar sempre o registo público de npm.
# Sem isto, um `npm install` numa máquina configurada para um registo interno
# grava esses URLs no package-lock.json e o build na nuvem falha por não lhes
# conseguir chegar.
registry=https://registry.npmjs.org/
